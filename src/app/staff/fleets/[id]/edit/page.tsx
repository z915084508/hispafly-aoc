import { notFound } from "next/navigation";import { prisma } from "@/lib/prisma";import { requireStaffPermission } from "@/lib/staff/authorization";import { FleetManagementForm } from "@/components/fleet-management-form";import { updateFleetAction } from "../../actions";
export default async function EditFleet({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{error?:string}>}){const {id}=await params;await requireStaffPermission("FLEET_EDIT",{entityType:"Fleet",entityId:id,attemptedAction:"edit fleet"});const [fleet,q]=await Promise.all([prisma.fleet.findUnique({where:{id}}),searchParams]);if(!fleet)notFound();return <><div className="page-header"><div><div className="eyebrow">VAMSYS FLEET</div><h1>Edit {fleet.code}</h1><p>Updates are published directly to vAMSYS.</p></div></div>{q.error&&<div className="notice error">{q.error}</div>}<FleetManagementForm action={updateFleetAction} value={fleet}/></>}
