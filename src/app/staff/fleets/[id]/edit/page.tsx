import{notFound,redirect}from"next/navigation";import{FleetManagementForm}from"@/components/fleet-management-form";import{findFleetById}from"@/lib/native-flight/fleet";import{requireStaffPermission}from"@/lib/staff/authorization";import{updateFleetAction}from"../../actions";export default async function Page({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{error?:string}>}){const{id}=await params;await requireStaffPermission("FLEET_EDIT",{entityType:"Fleet",entityId:id,attemptedAction:"edit Fleet"});const[x,q]=await Promise.all([findFleetById(id),searchParams]);if(!x)notFound();if(x.dataOrigin==="VAMSYS_LEGACY"||x.operationalStatus==="ARCHIVED")redirect(`/staff/fleets/${id}?error=Fleet%20is%20read-only.`);return <><h1>Edit {x.code}</h1>{q.error&&<div className="notice">{q.error}</div>}<FleetManagementForm action={updateFleetAction} value={x}/></>}
