import{notFound,redirect}from"next/navigation";import{AircraftManagementForm}from"@/components/aircraft-management-form";import{prisma}from"@/lib/prisma";import{findAircraftById}from"@/lib/native-flight/aircraft";import{requireStaffPermission}from"@/lib/staff/authorization";import{updateAircraftAction}from"../../actions";export default async function Page({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{error?:string}>}){const{id}=await params;await requireStaffPermission("AIRCRAFT_EDIT",{entityType:"Aircraft",entityId:id,attemptedAction:"edit Aircraft"});const[x,q,f]=await Promise.all([findAircraftById(id),searchParams,prisma.fleet.findMany({where:{operationalStatus:"ACTIVE",dataOrigin:{not:"VAMSYS_LEGACY"}},select:{id:true,code:true,name:true},orderBy:{code:"asc"}})]);if(!x)notFound();if(x.dataOrigin==="VAMSYS_LEGACY"||x.operationalStatus==="RETIRED")redirect(`/staff/aircraft/${id}?error=Aircraft%20is%20read-only.`);return <><h1>Edit {x.registration}</h1>{q.error&&<div className="notice">{q.error}</div>}<AircraftManagementForm action={updateAircraftAction} fleets={f} value={x}/></>}
