import{AircraftManagementForm}from"@/components/aircraft-management-form";import{prisma}from"@/lib/prisma";import{requireStaffPermission}from"@/lib/staff/authorization";import{createAircraftAction}from"../actions";export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){await requireStaffPermission("AIRCRAFT_CREATE",{entityType:"Aircraft",attemptedAction:"open Aircraft creation"});const[q,f]=await Promise.all([searchParams,prisma.fleet.findMany({where:{operationalStatus:"ACTIVE"},select:{id:true,code:true,name:true},orderBy:{code:"asc"}})]);return <><h1>New Aircraft</h1>{q.error&&<div className="notice">{q.error}</div>}<AircraftManagementForm action={createAircraftAction} fleets={f}/></>}
