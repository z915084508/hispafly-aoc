import {AircraftManagementForm} from "@/components/aircraft-management-form";import {prisma} from "@/lib/prisma";import {requireStaffPermission} from "@/lib/staff/authorization";import {createAircraftAction} from "../actions";
export default async function NewAircraft({searchParams}:{searchParams:Promise<{error?:string}>}){await requireStaffPermission("AIRCRAFT_CREATE",{entityType:"Aircraft",attemptedAction:"create aircraft"});const [q,fleets]=await Promise.all([searchParams,prisma.fleet.findMany({where:{vamsysFleetId:{not:null}},orderBy:{code:"asc"}})]);return <><div className="page-header"><div><div className="eyebrow">VAMSYS AIRCRAFT</div><h1>New aircraft</h1><p>Add an individual airframe and publish it directly to vAMSYS.</p></div></div>{q.error&&<div className="notice error">{q.error}</div>}<AircraftManagementForm action={createAircraftAction} fleets={fleets}/></>}
