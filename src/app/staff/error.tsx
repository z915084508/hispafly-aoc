"use client";import Link from "next/link";export default function StaffError({error,reset}:{error:Error&{digest?:string};reset:()=>void}){const forbidden=error.name==="StaffAuthorizationError"||/rol|permite|inactivo|active Staff|permission/i.test(error.message);return <div className="card forbidden-state"><div className="eyebrow">{forbidden?"ACCESS DENIED":"STAFF PORTAL"}</div><h1>{forbidden?"You do not have permission to view this page":"This page could not be loaded"}</h1><p>{forbidden?"Your Staff role does not include access to this module. No protected information has been disclosed.":"Please try again. If the problem continues, contact an AOC administrator."}</p><div className="button-row"><Link className="button" href="/staff">Back to Dashboard</Link>{!forbidden&&<button className="button secondary" onClick={reset}>Try again</button>}</div></div>}
