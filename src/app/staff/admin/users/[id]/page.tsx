import {notFound} from "next/navigation";import {prisma} from "@/lib/prisma";import {requireStaffPermission} from "@/lib/staff/authorization";import {PERMISSION_GROUPS} from "@/lib/staff/access/catalog";import {resolvePermissionCodes} from "@/lib/staff/access/resolve";import {setPermissionOverrideAction,updateStaffAction} from "../actions";
export default async function StaffDetail({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{success?:string;error?:string}>}){const {id}=await params;await requireStaffPermission("STAFF_VIEW",{entityType:"StaffUser",entityId:id,attemptedAction:"view Staff detail"});const [staff,roles,q,audit]=await Promise.all([prisma.staffUser.findUnique({where:{id},include:{roleTemplate:{include:{permissions:{include:{permission:true}}}},permissionOverrides:{include:{permission:true}}}}),prisma.staffRoleTemplate.findMany({where:{active:true,code:{not:"OWNER"}},orderBy:{displayOrder:"asc"}}),searchParams,prisma.aocAuditLog.findMany({where:{entityType:"StaffUser",entityId:id},orderBy:{createdAt:"desc"},take:20})]);if(!staff)notFound();const inherited=new Set(staff.roleTemplate?.permissions.map(x=>x.permission.code)??[]),override=new Map(staff.permissionOverrides.map(x=>[x.permission.code,x.effect])),effective=resolvePermissionCodes({legacyRole:staff.role,isSystemOwner:staff.isSystemOwner,rolePermissions:[...inherited],overrides:staff.permissionOverrides.map(x=>({code:x.permission.code,effect:x.effect}))});return <><div className="page-header"><div><div className="eyebrow">{staff.staffCode??"STAFF"}</div><h1>{staff.name}</h1><p>{staff.email} · {staff.roleTemplate?.name??`Legacy ${staff.role}`}</p></div></div>{q.success&&<div className="feedback success">{q.success}</div>}{q.error&&<div className="feedback error">{q.error}</div>}<form action={updateStaffAction} className="card settings-grid"><input type="hidden" name="id" value={id}/><label>Department<input name="department" defaultValue={staff.department??""}/></label><label>Job title<input name="jobTitle" defaultValue={staff.jobTitle??""}/></label><label>Role template<select name="roleTemplateId" defaultValue={staff.roleTemplateId??""}>{roles.map(r=><option key={r.id} value={r.id}>{r.name}</option>)}</select></label><label className="checkbox-row"><input name="active" type="checkbox" defaultChecked={staff.active}/> Active</label><label>Reason<input name="reason" placeholder="Required for role/status changes"/></label><button className="button">Save profile</button></form>{Object.entries(PERMISSION_GROUPS).map(([module,codes])=><section className="card" key={module}><h2>{module}</h2><div className="table-wrap"><table><thead><tr><th>Permission</th><th>Inherited</th><th>Override</th><th>Effective</th><th>Change</th></tr></thead><tbody>{codes.map(code=><tr key={code}><td>{code}</td><td>{inherited.has(code)?"Allow":"—"}</td><td>{override.get(code)??"Inherit"}</td><td>{effective.has(code)?"ALLOW":"DENY"}</td><td><form action={setPermissionOverrideAction} className="inline-action-form"><input type="hidden" name="id" value={id}/><input type="hidden" name="permissionCode" value={code}/><select name="effect" defaultValue={override.get(code)??"INHERIT"}><option>INHERIT</option><option>ALLOW</option><option>DENY</option></select><input name="reason" placeholder="Reason"/><button className="action-button approve">Apply</button></form></td></tr>)}</tbody></table></div></section>)}<section className="card"><h2>Permission history</h2>{audit.map(x=><p key={x.id}><strong>{x.action}</strong> · {x.createdAt.toLocaleString()}<br/><span className="meta">{x.message}</span></p>)}</section></>}
