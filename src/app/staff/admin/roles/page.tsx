import Link from "next/link";import {prisma} from "@/lib/prisma";import {requireStaffPermission} from "@/lib/staff/authorization";import {createRoleAction} from "./actions";
export default async function RolesPage(){await requireStaffPermission("ROLE_VIEW",{entityType:"StaffRoleTemplate",attemptedAction:"view role templates"});const [roles,permissions]=await Promise.all([prisma.staffRoleTemplate.findMany({include:{_count:{select:{permissions:true,users:true}}},orderBy:{displayOrder:"asc"}}),prisma.staffPermissionDefinition.findMany({orderBy:[{module:"asc"},{displayOrder:"asc"}]})]);return <><div className="page-header"><div><div className="eyebrow">ACCESS CONTROL</div><h1>Role templates</h1><p>System defaults and custom least-privilege roles.</p></div></div><div className="table-wrap"><table><thead><tr><th>Code</th><th>Name</th><th>Type</th><th>Status</th><th>Permissions</th><th>Users</th></tr></thead><tbody>{roles.map(r=><tr key={r.id}><td><Link href={`/staff/admin/roles/${r.id}`}><strong>{r.code}</strong></Link></td><td>{r.name}</td><td>{r.systemRole?"System":"Custom"}</td><td>{r.active?"Active":"Disabled"}</td><td>{r._count.permissions}</td><td>{r._count.users}</td></tr>)}</tbody></table></div><form action={createRoleAction} className="card route-form"><h2>Create custom role</h2><div className="route-form-grid"><label>Role code<input name="code" required placeholder="DISPATCH_SUPERVISOR"/></label><label>Name<input name="name" required/></label><label className="route-form-span-2">Description<input name="description"/></label><div className="route-form-span-2 role-permission-picker">{permissions.map(p=><label key={p.id}><input type="checkbox" name="permissionCode" value={p.code}/><span>{p.module} · {p.code}{p.dangerous?" ⚠":""}</span></label>)}</div></div><button className="button">Create role</button></form></>}
