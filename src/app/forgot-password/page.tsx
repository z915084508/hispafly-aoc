import Link from "next/link";import {forgotPasswordAction} from "./actions";export default async function ForgotPasswordPage({searchParams}:{searchParams:Promise<{success?:string}>}){const q=await searchParams;return <main className="portal-gateway admin-login-gateway"><section className="login-panel"><div className="login-copy"><span>ACCOUNT RECOVERY</span><h1>Forgot password</h1><p>Enter your verified Hispafly email address.</p><Link href="/login">Back to sign in</Link></div><form className="login-form" action={forgotPasswordAction}>{q.success&&<div className="feedback success">If the account exists, reset instructions have been sent.</div>}<label>Email<input name="email" type="email" required/></label><button className="button">SEND RESET LINK</button></form></section></main>}
