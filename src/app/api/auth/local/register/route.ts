import { registerPilot } from "@/lib/auth/service";
import { deliverIdentityToken } from "@/lib/auth/delivery";
export async function POST(request:Request){const body=await request.json().catch(()=>null) as Record<string,unknown>|null;if(!body)return Response.json({error:"invalid_request"},{status:400});try{const result=await registerPilot({firstName:String(body.firstName??""),lastName:String(body.lastName??""),email:String(body.email??""),username:String(body.username??""),password:String(body.password??""),callsign:typeof body.callsign==="string"?body.callsign:undefined});await deliverIdentityToken({type:"verify_email",email:result.user.email,token:result.token});return Response.json({ok:true,verificationRequired:true},{status:201});}catch(error){const message=error instanceof Error?error.message:"registration_failed";return Response.json({error:message.includes("Unique constraint")?"account_already_exists":message},{status:400});}}
