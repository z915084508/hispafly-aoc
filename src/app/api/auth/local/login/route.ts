import { loginWithPassword } from "@/lib/auth/service";
export async function POST(request:Request){const body=await request.json().catch(()=>null) as {email?:unknown;password?:unknown}|null;if(typeof body?.email!=="string"||typeof body.password!=="string")return Response.json({error:"invalid_request"},{status:400});const user=await loginWithPassword(body.email,body.password,{ipAddress:request.headers.get("x-forwarded-for")?.split(",")[0]?.trim(),userAgent:request.headers.get("user-agent")??undefined});return user?Response.json({ok:true}):Response.json({error:"invalid_credentials"},{status:401});}
