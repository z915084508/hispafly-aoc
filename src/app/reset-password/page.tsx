import {resetPasswordAction} from "./actions";export default async function ResetPasswordPage({searchParams}:{searchParams:Promise<{token?:string;error?:string}>}){const q=await searchParams;return <main className="portal-gateway admin-login-gateway"><section className="login-panel"><div className="login-copy"><span>ACCOUNT RECOVERY</span><h1>Set new password</h1><p>The reset link is single-use and expires after 30 minutes.</p></div><form className="login-form" action={resetPasswordAction}>{q.error&&<div className="feedback error">{q.error}</div>}<input type="hidden" name="token" value={q.token??""}/><label>New password<input name="password" type="password" minLength={12} required/></label><label>Confirm password<input name="confirmPassword" type="password" minLength={12} required/></label><button className="button" disabled={!q.token}>RESET PASSWORD</button></form></section></main>}
