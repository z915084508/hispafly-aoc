import { Badge, DataTable, Identity } from "@/components/data-table";
import { PageHeading } from "@/components/page-heading";
import { payroll } from "@/lib/mock-data";

export default function PayrollPage() { return <><PageHeading eyebrow="COMPENSACIÓN VIRTUAL" title="Nóminas" copy="Calculadas a partir de PIREPs aceptados mediante reglas AOC versionadas." action="Cerrar nómina de junio"/><section className="grid stats"><div className="card"><div className="stat-label">Periodo actual</div><div className="stat-value">Junio</div><div className="stat-note">Abierto para cálculo</div></div><div className="card"><div className="stat-label">Total acumulado</div><div className="stat-value">92.480 €</div><div className="stat-note">Moneda virtual</div></div><div className="card"><div className="stat-label">PIREPs aptos</div><div className="stat-value">184</div><div className="stat-note">Solo aceptados</div></div><div className="card"><div className="stat-label">Requieren revisión</div><div className="stat-value">3</div><div className="stat-note">Atención manual</div></div></section><div className="card" style={{marginTop:18}}><DataTable headers={["Piloto", "Periodo", "Vuelos", "Horas", "Salario base", "Bonificaciones", "Total", "Estado"]} rows={payroll.map((p) => [<Identity key="i" primary={p.pilot} secondary="Junio de 2026"/>, "Junio de 2026", p.flights, p.hours.toFixed(1), `${p.base.toLocaleString("es-ES")} €`, `${p.bonus.toLocaleString("es-ES")} €`, <strong key="t">{p.total.toLocaleString("es-ES")} €</strong>, <Badge key="b" tone="blue">Borrador</Badge>])}/></div></> }
