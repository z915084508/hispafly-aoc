import {operationsRequest} from "@/lib/vamsys/operations";import type {AircraftPayload,VamsysAircraftData} from "./types";const unwrap=(v:unknown)=>((v as {data?:VamsysAircraftData})?.data??v) as VamsysAircraftData;
export async function listFleetAircraft(fleetId:string){const rows:VamsysAircraftData[]=[];let cursor:string|null=null;do{const q=new URLSearchParams({"page[size]":"100",sort:"id",weight_unit:"kg"});if(cursor)q.set("page[cursor]",cursor);const p=await operationsRequest(`/fleet/${encodeURIComponent(fleetId)}/aircraft?${q}`) as {data:VamsysAircraftData[];meta?:{next_cursor?:string|null}};rows.push(...(p.data??[]));cursor=p.meta?.next_cursor??null}while(cursor);return rows}
export const createAircraft=async(f:string,b:AircraftPayload)=>unwrap(await operationsRequest(`/fleet/${encodeURIComponent(f)}/aircraft`,{method:"POST",body:b,expectedStatus:201,requiredScope:"ops:config:write"}));
export const updateAircraft=async(f:string,a:string,b:AircraftPayload)=>unwrap(await operationsRequest(`/fleet/${encodeURIComponent(f)}/aircraft/${encodeURIComponent(a)}?weight_unit=kg`,{method:"PUT",body:b,expectedStatus:200,requiredScope:"ops:config:write"}));
export const deleteAircraft=async(f:string,a:string)=>operationsRequest(`/fleet/${encodeURIComponent(f)}/aircraft/${encodeURIComponent(a)}`,{method:"DELETE",expectedStatus:204,requiredScope:"ops:config:write"});
