export function greatCircleDistanceNm(a:{latitude:number;longitude:number},b:{latitude:number;longitude:number}){const rad=(v:number)=>v*Math.PI/180;const dLat=rad(b.latitude-a.latitude),dLon=rad(b.longitude-a.longitude);const h=Math.sin(dLat/2)**2+Math.cos(rad(a.latitude))*Math.cos(rad(b.latitude))*Math.sin(dLon/2)**2;return Math.max(1,Math.round(3440.065*2*Math.asin(Math.sqrt(h))));}
export const estimatedRouteMinutes=(distanceNm:number)=>Math.max(30,Math.round(distanceNm/430*60+30));
export function arrivalTimeUtc(departure:string,durationMinutes:number){if(!/^\d{2}:\d{2}/.test(departure)||!Number.isFinite(durationMinutes))return "";const [hours,minutes]=departure.split(":").map(Number);const total=(hours*60+minutes+durationMinutes)%(24*60);return `${String(Math.floor(total/60)).padStart(2,"0")}:${String(total%60).padStart(2,"0")}:00`;}
export function nextRouteIdentity(items:Array<{flightNumber:string|null;callsign:string|null}>){const flights=new Set(items.map(x=>x.flightNumber).filter(Boolean)),callsigns=new Set(items.map(x=>x.callsign).filter(Boolean));for(let number=100;number<=999;number++){const flightNumber=`HF${number}`,callsign=`HPF${number}`;if(!flights.has(flightNumber)&&!callsigns.has(callsign))return{flightNumber,callsign};}throw new Error("No unused HF/HPF route identities remain between 100 and 999.");}
