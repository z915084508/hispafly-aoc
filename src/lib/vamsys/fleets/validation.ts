import { FLEET_TYPES,type FleetFormInput,type FleetType } from "./types.ts";
const text=(form:FormData,key:string)=>String(form.get(key)??"").trim(); const integer=(form:FormData,key:string)=>{const value=text(form,key);if(!value)return undefined;const parsed=Number(value);if(!Number.isInteger(parsed)||parsed<0)throw new Error(`${key} must be a non-negative integer.`);return parsed;};
export function validateFleetForm(form:FormData):FleetFormInput{const name=text(form,"name"),code=text(form,"code").toUpperCase(),type=text(form,"type") as FleetType;if(!name||name.length>255)throw new Error("Fleet name is required and must not exceed 255 characters.");if(!/^[A-Z0-9]{4}$/.test(code))throw new Error("Fleet ICAO code must contain exactly four letters or numbers.");if(!FLEET_TYPES.includes(type))throw new Error("Invalid fleet type.");const imageLinkback=text(form,"imageLinkback");if(imageLinkback){const url=new URL(imageLinkback);if(!/^https?:$/.test(url.protocol))throw new Error("Image link must be an HTTP(S) URL.");}return{localId:text(form,"id")||undefined,name,code,type,maxPassengers:integer(form,"maxPassengers"),maxCargoKg:integer(form,"maxCargoKg"),containerUnits:integer(form,"containerUnits"),hiddenInPhoenix:form.get("hiddenInPhoenix")==="on",scoringGroupId:integer(form,"scoringGroupId"),simbriefAircraftProfiles:text(form,"simbriefAircraftProfiles").split(/[\s,]+/).filter(Boolean),parameterIds:text(form,"parameterIds").split(/[\s,]+/).filter(Boolean),imageAttribution:text(form,"imageAttribution")||undefined,imageLinkback:imageLinkback||undefined,internalNotes:text(form,"internalNotes")||undefined};}
