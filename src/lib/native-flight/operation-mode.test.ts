import assert from "node:assert/strict";import{readFileSync}from"node:fs";import{fileURLToPath}from"node:url";import{validateProposedScheduleWithContext}from"../native-scheduling/validation.ts";
const proposed={routeId:"route",daysOfWeek:[1],departureTimeMinutesUtc:480,arrivalTimeMinutesUtc:540,scheduledDurationMinutes:60,defaultFleetId:"fleet",assignedAircraftId:"aircraft",effectiveFrom:new Date("2026-08-10"),effectiveUntil:null,bookingOpenOffsetMinutes:10080,bookingCloseOffsetMinutes:60,generationHorizonDays:30};const route={id:"route",active:true,operationalStatus:"ACTIVE",archivedAt:null,departureAirportId:"a",arrivalAirportId:"b",departureAirport:{id:"a",icao:"LEMD",status:"ACTIVE"},arrivalAirport:{id:"b",icao:"LEVC",status:"ACTIVE"},fleetAssignments:[{fleetId:"fleet"}],fleetCompatibility:[]};const fleet={id:"fleet",operationalStatus:"ACTIVE",active:true,archivedAt:null};const aircraft=(operationMode:"FREE"|"SCHEDULED"|"FLEX")=>({id:"aircraft",nativeFleetId:"fleet",operationalStatus:"AVAILABLE",operationMode,archivedAt:null,conditionSnapshot:null,nativeFleet:fleet});const validate=(mode:"FREE"|"SCHEDULED"|"FLEX")=>validateProposedScheduleWithContext(proposed,{route,fleet,aircraft:aircraft(mode),existingSchedules:[],generatedFlights:[]});assert.equal(validate("FREE").valid,false);assert.ok(validate("FREE").errors.some(i=>i.code==="AIRCRAFT_FREE_ONLY"));assert.equal(validate("SCHEDULED").valid,true);assert.equal(validate("FLEX").valid,true);assert.match(readFileSync(fileURLToPath(new URL("./self-dispatch.ts",import.meta.url)),"utf8"),/operationMode === "SCHEDULED"/);assert.match(readFileSync(fileURLToPath(new URL("./booking.ts",import.meta.url)),"utf8"),/operationMode === "FREE"/);console.log("Aircraft operation mode contracts passed.");
